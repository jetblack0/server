---
- name: Refresh the apt cache when it is stale
  become: true
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install the base packages with apt
  become: true
  ansible.builtin.apt:
    name: "{{ packages_selected }}"
    state: present
