# Only picked up when running from this directory. ansible-pull runs from its
# own checkout root and never sees this file, so nothing here may be
# load-bearing -- what has to hold in every model lives in group_vars/all.yml.
[defaults]
inventory = inventory
localhost_warning = false

[inventory]
inventory_unparsed_warning = false
